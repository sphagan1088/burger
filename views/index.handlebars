{{!-- Setup the index.handlebars to have the template that Handlebars can render onto.

Create a button in index.handlebars that will submit the user input into the database. --}}

<div class="container">

<img src="../assets/img/burger.png">

<h1>Eat That Burger!</h1>

{{!-- Need to be able to show the data in the database on the HTML page 
    Could not figure out the right syntax to do this.--}}
{{!-- 
    Need to make the devour button and use the correct syntax to display it in the devoured column. --}}

{{!-- When a burger is added it should POST it to the database --}}

<form class="create-form">
  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>
   <button type="submit">Add Burger</button>
</form>
